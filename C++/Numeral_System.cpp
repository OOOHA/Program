#include<iostream>
#include<math.h>
#include<bitset>
#include<string.h>

using namespace std;

int main()
{
    /*cout << "Please enter the number that you want to change:";
    double number_input;
    cin >> number_input;
    cout << "Please enter what system you need:";
    int system;
    cin >> system;
    int count = 0;
    
    int number_input_count = static_cast<int>(number_input);
    
    while (number_input_count > 0)
    {
        number_input_count = number_input_count / 10;
        count++;
    }
    
    for (int i = 0; i < count; i++)
    {
        int find_out_digits;
        find_out_digits = pow(10, i);
        number_input / find_out_digits;
    }

    if(system == 2)
    {
        for (int i = 0; i > count; i++)
        {
            
        }
        
    }
    
    

    /*cout << number_input << " to Binary: " << bitset<8>(number_input) << endl;
      
    //cout << number_input << " to Decimal" << dec << number_input << endl;
    cout << number_input << " to Hexadecimal:" << hex << number_input << endl;
    cout << number_input << " to Octal:" << oct << number_input<< endl;*/
    int a;
    cin >> a;
    string b;
    cin >> b;
    cout << b;

    return 0;
}