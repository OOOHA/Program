P.115 3 4章下次段考會考

find /usr/foo -name "*.c"
    (絕對路徑) (檔案名) (副檔名)

find /usr/foo -name "*.c" > list
存檔

find /usr/foo -name "s*.c"
找出s開頭副檔名為c

find /usr/foo -name "s*.c" -exec grep -nH 'main' {} \;
                        (給下一個命令執行)  (找檔案裡面有沒有main這個字在裡面)

find /usr/foo -name "s*.c" -exec grep -nH -w 'main' {} \;
                                         (只要找main這個字)

find /usr/foo -name "s*.c" -exec cp {} . \;
把找到的檔案全部貼到現在工作的目錄

tar -cf FILE.tar *.h
打包

tar -xf ../FILE.tar
解開

ps ax
看開機後跑的程式

echo date
把東西(date)show到螢幕上

echo可利用 > 把東西存進檔案
e.g.  echo date > date.sh
如果用>>則可以變成兩行

cat
讀檔

touch chap chap01 chap02 chap03 chapa chapb chapc

ls chap*

ls chap?

ls chap0[12]

ls chap[abc]

ls chap[a-c]

ls chap0[1-3]

ls chap0![1-3]

car -test
-代表使用者輸入

foo.sh > barl >> barz
foo.sh執行正常訊息傳到barl錯誤訊息傳到

P172
用()可以一次把很多指令收集在一起一起打




P147

cat stdio.h | tee bak.txt
tee的作用是將前面程式執行的結果存擋備份


count 5+6
echo count
count='hello world'
echo $count

total=$count
把count內容copy到total

P180

cat filelist

cat > script.sh
使用者輸入後ctrl b 結束

cat script.sh
出現剛剛輸入的資料


gcc -c main main.c